export default class Tarefa {
  constructor (descricao, dataTermino, prioridade, situacao) {
    this.descricao = descricao
    this.dataTermino = dataTermino
    this.prioridade = prioridade
    this.situacao = situacao
  }
}
